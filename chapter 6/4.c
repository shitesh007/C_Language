/*Write a function to calculate the square of a number.*/
#include<stdio.h>
int square(int a){
    return a*a;
}
int main()
{
int a=5;
 int reuslt=square(5);
 printf("the square is %d",reuslt);
return 0;
}